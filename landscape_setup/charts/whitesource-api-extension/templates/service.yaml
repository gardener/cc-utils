apiVersion: v1
kind: Service
metadata:
  labels:
    app: whitesource-api-extension
  name: whitesource-api-extension
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - name: web
    port: 80
    protocol: TCP
    targetPort: 8000
  selector:
    app: whitesource-api-extension
  sessionAffinity: None
  type: ClusterIP
