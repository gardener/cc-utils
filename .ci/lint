#!/bin/bash -e

set -e

# fake an error for testing purposes
echo "faking an error"
exit 1

# usage: $1: <src-dir>

src_dir="${1:-"$(readlink -f "$(dirname "${0}")/..")"}"

python3 $(which pylint3) -j 4 --errors-only \
  ${src_dir}/**/*.py

