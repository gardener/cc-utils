cc-utils:
  template: 'default'
  base_definition:
    steps:
      pylint-lint: ~
      pyflakes-lint: ~
      test: ~
  variants:
    failing:
      repo:
        trigger: false
      steps:
        failing:
          publish_to: ['source']
    head_update: ~
    pull_request:
      traits:
        pull-request:
          repo: 'source'  # default
          policies:
            require-label: 'reviewed/ok-to-test' # default
            build-forks: true # default
